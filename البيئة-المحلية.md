# تشغيل المشروع محليًا — سجل المبيعات والديون (nexora_app)

تطبيق **Flutter** لنظام محاسبي (إدارة مبيعات وديون ومخزون ونسخ احتياطي ومزامنة).
تم تجهيزه وتشغيله بنجاح في البيئة المحلية على **Linux desktop**.

## البيئة المثبّتة

| المكوّن | الإصدار |
|---|---|
| Flutter | 3.47.2 (stable) |
| Dart | 3.13.2 |
| أداة Linux | clang 19 / cmake 3.31 / ninja 1.12 + GTK3 |
| المسار | `/opt/flutter/bin` (مُضاف إلى PATH) |

> ملاحظة: التطبيق يستخدم قاعدة بيانات SQLite عبر `sqflite_common_ffi` على سطح
> المكتب، ويستورد `dart:io` مباشرة — لذلك **الويب غير مدعوم**؛ المنصة هي
> Linux / Windows / macOS + Android / iOS.

## الأوامر السريعة

```bash
cd sijil-al-mabiat-wal-duyun

./run.sh      # تشغيل النسخة المبنية (release) — يستخدم Xvfb تلقائيًا إن لم توجد شاشة
./dev.sh      # وضع التطوير مع hot reload على Linux
./build.sh    # إعادة البناء (flutter pub get + flutter build linux --release)
```

أو يدويًا:

```bash
export PATH="/opt/flutter/bin:$PATH"
flutter pub get
flutter run -d linux          # تطوير
flutter build linux --release # بناء
./build/linux/x64/release/bundle/nexora_app   # تشغيل
```

## موقع النسخة الجاهزة

```
build/linux/x64/release/bundle/
├── nexora_app      # الملف التنفيذي
├── lib/            # مكتبات Flutter المرفقة
└── data/           # أصول التطبيق (flutter_assets)
```

> مجلد `build/` لا يُحفظ في لقطة مساحة العمل (يُعاد بناؤه بـ `./build.sh`).

## الاختبارات

```bash
flutter test        # ✅ 167 اختبارًا — جميعها ناجحة
flutter doctor      # فحص الجاهزية
```

## البيانات

قاعدة البيانات تُنشأ تلقائيًا عند أول تشغيل في مجلد دعم التطبيق
(`getApplicationSupportDirectory`)، أي عادةً تحت:
`~/.local/share/nexora_app/` (أو معرف التطبيق `com.nexora.nexora_app`).
