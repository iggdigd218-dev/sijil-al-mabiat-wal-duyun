# تقرير فحص شامل — سجل المبيعات والديون (nexora_app)

**تاريخ الفحص:** 2026-09-07 · **الإصدار:** 3.12.3+20 · **Flutter 3.47.2 / Dart 3.13.2**
**منصة الفحص:** Linux desktop · **حجم الكود:** ~26,700 سطر Dart في `lib/`

---

## ١) نظرة عامة

تطبيق محاسبي متكامل باللغة العربية (RTL) يدير: الحسابات (عملاء/موردون/صناديق)، العمليات
المالية، السندات، نقطة البيع، المخزون، العملات المتعددة، التقارير، المستخدمين والصلاحيات،
الدردشة، المزامنة بين الأجهزة، والنسخ الاحتياطي السحابي والمحلي.

طبقة البيانات محلية عبر **SQLite (sqflite + FFI على سطح المكتب)** مع اشتقاق مُسبق
للنسخة الويب (IndexedDB في النسخة القديمة).

---

## ٢) النوافذ / الشاشات

### أ) الشاشات الرئيسية — ١٣ نافذة في شريط التنقل (`home_shell.dart` ← `enum AppScreen`)

| # | النافذة | الملف | الوظيفة |
|---|---|---|---|
| 1 | لوحة التحكم | `dashboard_screen.dart` | ملخص: الإيرادات/المصروفات، الذمم، أكثر الحسابات مديونية، إجمالي المبالغ |
| 2 | نقطة البيع POS | `pos_screen.dart` (1250 سطر) | فاتورة، سلة، باركود، إتمام، إيصال حراري 80mm، واتساب |
| 3 | الحسابات | `accounts_screen.dart` | قائمة عملاء/موردين/صناديق + بحث وفلترة |
| 4 | العمليات | `transactions_screen.dart` | سجل الحركات المالية (823 سطر) |
| 5 | السندات | `vouchers_screen.dart` | سندات قبض/صرف/قيد/تحويل مع اعتماد |
| 6 | التقارير | `reports_screen.dart` | تقارير PDF/CSV، أرباح/خسائر، ذمم، إيرادات ومصروفات |
| 7 | المخزون والأصناف | `inventory_screen.dart` (1162 سطر) | أصناف، فئات، جرد، تسوية، ربح محقق، تنبيه نواقص |
| 8 | العملات | `currencies_screen.dart` | إدارة YER/USD/SAR وأسعار الصرف |
| 9 | الدردشة | `chat_screen.dart` | محادثات بين الأجهزة/المستخدمين |
| 10 | إدارة المجموعة | `group_management_screen.dart` (670 سطر) | الأجهزة والمستخدمين المشتركين في الـ workspace |
| 11 | سلة المهملات | `trash_screen.dart` | حذف ناعم مع استرجاع ذرّي |
| 12 | سجل النشاط | `activity` داخل home_shell | تتبّع من فعل ماذا |
| 13 | الإعدادات | `settings_screen.dart` | الإعدادات العامة + أقسام مدمجة |

### ب) الشاشات/النوافذ الفرعية والحوارات

- **`splash.dart`** — شاشة بدء/تهيئة.
- **`lock_gate.dart`** — بوابة قفل (PIN / بصمة عبر `local_auth`).
- **`account_detail.dart`** (422) — تفاصيل حساب + رصيد وكشف حساب.
- **`account_form.dart`** — إضافة/تعديل حساب.
- **`users_screen.dart`** (594) — إدارة المستخدمين والأدوار.
- **`devices_screen.dart`** (759) — الأجهزة المرتبطة وربط/طرد.
- **`backup_screen.dart`** (1017) — نسخ احتياطي: Google Drive، ملف محلي، استعادة.
- **`sync_settings_section.dart`** (1324 سطر، الأكبر) — إعداد المزامنة والإقران بـ QR.
- **`update_section.dart` / `update_service.dart`** — فحص وتحديث التطبيق.
- **`calculator.dart`** — آلة حاسبة (`math_expressions`).
- **`barcode_scanner.dart` / `qr_pair_scanner.dart`** — مسح باركود/إقران (`mobile_scanner`).
- **`voucher_doc.dart`** — مستند سند قابل للطباعة/مشاركة.
- **`tx_share.dart`** — مشاركة فاتورة/إيصال (واتساب/صور).
- **`widgets.dart`** (405) — عناصر واجهة مشتركة.

> إجمالي ٢٠ صنف شاشة من نوع `*Screen`، إضافة إلى حوارات `showDialog`/`showModalBottomSheet`
> موزعة على 21 ملفاً (أكثرها في العمليات وإعدادات المزامنة).

---

## ٣) البيانات — قاعدة SQLite

**الملف:** `~/.local/share/com.nexora.nexora_app/nexora.db` · **إصدار المخطط:** 16 ·
**الجداول:** 23 · **الفهارس:** 19 · **المفاتيح الأجنبية:** مفعّلة (`PRAGMA foreign_keys = ON`).

### أ) مجموعة المزامنة (8 جداول)
| الجدول | الغرض |
|---|---|
| `workspaces` | مساحة عمل/متجر (المالك Google) |
| `devices` | الأجهزة المرتبطة (منصة، IP/منفذ، رموز إقران/مصادقة) |
| `operations` | سجل عمليات المزامنة (CRDT-like: entity, op_type, version, payload) |
| `sync_queue` | طابور عمليات معلّقة مع محاولات/أخطاء |
| `sync_meta` | بيانات وصفية (رقم المخطط، آخر مزامنة) |
| `google_auth` | رمز حساب Google (صف واحد id=1) |
| `conversations` | عناوين الدردشة |
| `messages` | رسائل الدردشة (نص/صور) |

### ب) المجال المحاسبي (15 جدول)
| الجدول | أبرز الحقول |
|---|---|
| `accounts` | kind، opening_balance، currency، phone/whatsapp، credit_limit، tags، archived، soft-delete |
| `transactions` | type، amount (CHECK>0)، currency، rate، from_id/to_id، status، sync_state، 3 FK للحسابات |
| `transaction_items` | سطور الفاتورة: item، quantity، unit_price، total (قيود CHECK) |
| `vouchers` | number، kind (5 أنواع)، account_id، tx_id، status (draft/معتمد) |
| `currencies` | code (PK)، name، symbol، decimal، rate |
| `categories` | تصنيفات الحسابات (scope=account) |
| `item_categories` | فئات الأصناف (منفصلة) |
| `items` | sku، buy/sell_price، quantity، min_quantity (تنبيه نواقص) |
| `stock_moves` | kind (شراء/بيع/تسوية/مرتجع)، quantity، unit_price |
| `users` | role، pin/password (مشفّرة)، permissions، is_me |
| `settings` | key/value (السمة، إخفاء الأرصدة، معرّف الجهاز…) |
| `templates` | قوالب رسائل |
| `notifications` | إشعارات داخلية |
| `activity` | سجل النشاط |
| `trash` | سلة المهملات (store + payload JSON) — حذف ناعم |

**الحذف الناعم:** كل الكيانات الأساسية تحمل `deleted_at` / `deleted_by` / `restore_op_id`
بدل الحذف الفعلي، مع جداول `trash` للاسترجاع الذرّي.
**العلاقات:** 10 مفاتيح أجنبية مع `ON DELETE CASCADE` (معاملات→حسابات، سطور→فواتير،
حركات مخزون→أصناف، رسائل→محادثات، طابور→عمليات…).

### ج) البيانات الأولية بعد أول تشغيل (حالة فعلية من القاعدة)
| الجدول | المحتوى |
|---|---|
| `workspaces` | متجر افتراضي واحد: `default` — «متجري» |
| `users` | مستخدم واحد: «المدير» (admin, is_me=1) بكل الصلاحيات |
| `currencies` | 3 عملات: الريال اليمني YER (0 كسور)، الدولار USD، الريال السعودي SAR |
| `settings` | `sync.deviceId = DEVICE-73HPRSBB`، `sync.workspaceId = default` |
| `sync_meta` | `schemaVersion = 16` |
| الباقي | فارغ (0 صف) — جاهز لإدخال البيانات |

---

## ٤) نماذج الكائنات (`models.dart`، 996 سطر)

| الكائن | أبرز ما فيه |
|---|---|
| `Account` | الحساب + نوعه (عميل/مورد/صندوق) |
| `Tx` | العملية المالية |
| `InvoiceLine` | سطر فاتورة |
| `AccountWithBalance` | حساب مع رصيده المحسوب |
| `Voucher` + `VoucherKind` | سند و5 أنواع |
| `AppUser` + `UserRole` + `Perm` | مستخدم ودور وصلاحية |
| `ChatMessage` | رسالة دردشة |
| `Item` / `ItemCategory` / `StockMove` + `StockKind` | مخزون |

**التعدّدات (enums) الجوهرية:**
- **`OpType` (8 عمليات):** قبض 🔄، صرف 💸، عليه(مدين) 🔴، له(دائن) 🟢، إيراد 📈، مصروف 📉، تحويل 🔁، تسوية ⚖️.
- **`VoucherKind` (5 سندات):** قبض 🧾، صرف 💳، قيد مدين 📥، قيد دائن 📤، تحويل 🔁.
- **`UserRole` (4 أدوار):** مدير 👑 / محاسب 🧮 / إدخال ⌨️ / عرض فقط 👁.
- **`StockKind` (4 حركات):** شراء(+)، بيع(−)، تسوية، مرتجع(+).

### منطق الرصيد (`accounting.dart`)
- الرصيد **الموجب (+)** = مبلغ «**عليه**» (مستحق لنا) · **السالب (−)** = «**له**» (مستحق علينا).
- `opEffect`: القبض/الصرف يختلف أثرهما بين الحساب العام (الصندوق) والعميل/المورد
  (القبض من العميل سداد يُنقص ذمته). التحويل له ساقان، والتسوية علامتها يختارها المستخدم.

---

## ٥) الصلاحيات (8) — افتراضية لكل دور

| الصلاحية | مدير | محاسب | إدخال | عرض |
|---|:-:|:-:|:-:|:-:|
| إضافة عمليات | ✅ | ✅ | ✅ | ❌ |
| تعديل عمليات | ✅ | ✅ | ✅ | ❌ |
| حذف عمليات | ✅ | ✅ | ❌ | ❌ |
| عرض التقارير | ✅ | ✅ | ❌ | ✅ |
| تصدير البيانات | ✅ | ✅ | ❌ | ❌ |
| إدارة النسخ الاحتياطي | ✅ | ❌ | ❌ | ❌ |
| إدارة المستخدمين | ✅ | ❌ | ❌ | ❌ |
| اعتماد/إلغاء السندات | ✅ | ✅ | ❌ | ❌ |

> توافق خلفي: دور `manager` القديم يُعاد إلى `admin` تلقائياً؛ ويوجد حارس
> `_guardSingleAdmin` يمنع حذف/تعطيل آخر مدير.

---

## ٦) المزامنة والنسخ الاحتياطي

- **محرك مزامنة** (`sync/`): محرك، طابور، مسجّل عمليات (recorder)، حل تعارضات
  (conflict_resolver)، تطبيق عن بُعد.
- **3 قنوات نقل:** سحابة Firebase (`cloud_firebase_transport`)، شبكة محلية LAN
  (`lan_http_transport` — خادم HTTP مع IP/منفذ)، Google Drive.
- **الإقران:** عبر QR (`qr_pairing`) و«مصادقة Google».
- **النسخ الاحتياطي (`backup_screen`):** تصدير JSON كامل (مع الصور) / استعادة ذرّية
  (صف غير صالح يُعيد القاعدة السابقة)، رفع/سحب على Google Drive، مشاركة ملف.

---

## ٧) نتائج الفحص التقني

| الفحص | النتيجة |
|---|---|
| البناء `flutter build linux --release` | ✅ نجح |
| التشغيل الفعلي | ✅ يعمل (Impeller/GTK) وينشئ القاعدة |
| الاختبارات `flutter test` | ✅ **167/167 ناجحة** |
| `flutter analyze` | ⚠️ **76 ملاحظة، كلها `info`** (لا أخطاء/تحذيرات) |
| تعليقات TODO/FIXME/HACK | ✅ لا يوجد أي منها |
| المفاتيح الأجنبية والفهارس | ✅ مفعّلة (19 فهرساً، 10 FK + CASCADE) |
| قيود سلامة البيانات | ✅ CHECK على المبالغ والكميات |
| تشفير كلمة المرور | SHA-256 مع ملح ثابت (`security.dart`) |

### ملاحظات وتوصيات خفيفة
1. **ملاحظات `analyze` (info فقط):** أغلبها `prefer_const_constructors` و
   `curly_braces_in_flow_control_structures` في `tx_form.dart`/`voucher_doc.dart` — تجميلية،
   لا تؤثر على الأداء الوظيفي؛ تُعالَج بـ `dart fix --apply`.
2. **تجزئة كلمة المرور:** SHA-256 بملح ثابت عام (`nexora::`) كافٍ لقفل محلي، لكن
   لبيئة متعددة مستخدمين يُفضّل ملح فريد لكل مستخدم + خوارزمية بطيئة (مثل PBKDF2/Argon2).
3. **العملات الافتراضية** سعر صرفها `1.0` — يجب ضبط أسعار USD/SAR مقابل YER من شاشة العملات
   قبل استخدام التقارير متعددة العملات.
4. القاعدة الجديدة **فارغة** (متجر + مدير + 3 عملات فقط) — هذا سلوك الإقلاع الأول الطبيعي.

---

## خلاصة

المشروع **مكتمل وسليم يعمل محلياً**؛ بنية بيانات واضحة (23 جدولاً مع علاقات وفهارس)،
13 نافذة رئيسية + نوافذ فرعية تغطي دورة محاسبية كاملة (مبيعات، ذمم، مخزون، سندات،
تقارير، صلاحيات، مزامنة، نسخ احتياطي)، والاختبارات والتحليل يمران دون أخطاء.
