{
  "name": "e4-data-accounting",
  "version": "3.4.0",
  "description": "إدارة البيانات — تطبيق محاسبي عربي متكامل (ويب + أندرويد)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "build": "node scripts/prepare-www.js",
    "build:www": "node scripts/prepare-www.js",
    "test": "node test/smoke.mjs && node test/dom.mjs",
    "start": "node server.js",
    "sync:android": "npm run build:www && npx cap sync android",
    "build:android": "npm run sync:android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^24.1.3"
  }
}
