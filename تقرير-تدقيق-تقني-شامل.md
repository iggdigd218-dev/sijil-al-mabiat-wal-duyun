# تقرير تدقيق تقني شامل — نكسورا (سجل المبيعات والديون)

**التاريخ:** 2026-09-07 · **الإصدار:** 3.12.3+20 (versionCode 20)
**البيئة:** Flutter 3.47.2 / Dart 3.13.2 · Linux x64 · **الحزمة:** `com.nexora.eradata` («إدارة البيانات»)
**نطاق التدقيق:** الكود المصدري `lib/` (~26.7k سطر)، قاعدة البيانات الفعلية، الأمان، المزامنة،
النسخ الاحتياطي، الواجهات، التبعيات، إعدادات CI/Android، والوثائق الداخلية (`qa/`, `docs/`).

> المنهجية: قراءة الكود + استعلام قاعدة SQLite الفعلية + `flutter analyze`/`test` + فحص الـ APK الرسمي.
> لا يُسلَّم هنا رأيٌ يعتمد فقط على الوثائق؛ كل بند خضع للتحقق من الكود الحالي.

---

## أ) خلاصة تنفيذية

المشروع **ناضج وجودته عالية**: بنية نظيفة (طبقات core/data/ui)، معاملة بيانات ذرّية،
معرّفات عالمية، نظام صلاحيات fail-closed، مزامنة محلية-أولاً مُحصّنة، و**167 اختباراً ناجحاً**
منها اختبارات قبول لمانعات النشر. غالبية العيوب التي رصدها فريق QA الداخلي (12 مرشّحاً + مانعات)
**أُصلحت فعلاً في الكود الحالي** وتحقّقتُ من ذلك سطراً بسطر.

تبقى **ملاحظتان أمنيتان حقيقيتان** (قواعد Firebase متعددة المستأجرين، وتجزئة كلمة المرور)،
وعدد من الملاحظات التشغيلية (ترقية تبعيات، توضيح مساري المزامنة، وحدود بيئة التشغيل).

| المحور | الحالة |
|---|---|
| البناء والتشغيل (Linux + APK رسمي) | ✅ يعمل |
| الاختبارات | ✅ 167/167 (بينها 4 مانعات نشر + 32 شاشة) |
| `flutter analyze` | ⚠️ 76 ملاحظة `info` فقط (لا أخطاء/تحذيرات) |
| سلامة المعاملات والذرّية | ✅ ممتازة |
| الصلاحيات (fail-closed) | ✅ مُصلَحة |
| أمان LAN | ✅ مُحصّن (مصادقة + صلاحيات + حد حمولة) |
| أمان Firebase السحابي (قواعد RTDB) | 🔴 **ضعيف: لا عزل بين المستأجرين** |
| تخزين كلمة المرور | 🟠 ملح ثابت عام + SHA-256 (قابل للتحسين) |
| صيانة التبعيات | 🟠 34 حزمة متخلّفة عن الأحدث (لا ثغرات مؤكَّدة) |
| حقن SQL | ✅ غير موجود (استعلامات مُحدَّدة المعاملات) |
| أسرار مكشوفة في المستودع | ✅ لا يوجد |

---

## ب) البنية المعمارية

- **النمط:** Local-first. تخزين SQLite عبر `sqflite`/`sqflite_common_ffi` (FFI على سطح المكتب).
- **الحالة:** Riverpod (`ProviderScope` مع حقن `Repo` و`SyncEngine`).
- **الطبقات:**
  - `core/` — النماذج، المحاسبة، قاعدة البيانات/المخطط (إصدار 16)، الأمان، التنسيق، الثيم.
  - `data/repository.dart` (2659 سطر) — كل منطق الوصول للبيانات والمعاملات.
  - `data/sync/` — محرك مزامنة، طابور، مسجّل عمليات (Op-log)، ناقل سحابي Firebase REST،
    ناقل شبكة LAN (خادم/عميل HTTP)، حل تعارضات، إقران QR، هوية جهاز/مستخدم Google.
  - `ui/` — 20 صنف شاشة + حوارات وعناصر مشتركة.
- **المزامنة:** سجل عمليات (operations) مع version/timestamp، طابور إعادة محاولة بـ backoff،
  دفع فوري (200ms) + دورة دورية + صيانة 6 ساعات، وحل تعارض حاسم (deterministic).

---

## ج) تحقّق من إصلاحات QA الداخلية (الحالة الفعلية في الكود)

وثائق `qa/ISSUES_STAGE4.md` رصدت 12 مرشّحاً و4 مانعات نشر. تحقّقت من كل منها:

| المرشح/المانع | الحالة في الكود الحالي | الدليل |
|---|---|---|
| **PERM-FAILOPEN** الصلاحيات تمر عند الخطأ/المهلة | ✅ **أُصلح (fail-closed)** | `repository.dart`: `can()` يلتقط الخطأ ويعيد `false`؛ `_ensureCan` مهلتها `onTimeout: () => false` |
| **FORMAT** `1.2.3` يُحوَّل `1.23` | ✅ **أُصلح** | `format.dart parseAmount`: يتعقّب `seenDot` ويعيد `null` عند فاصلة مزدوجة |
| **SAVE-RACE** ضغطتان خلال 50ms | ✅ **مُصلَح** | `tx_form.dart:187` القفل يُؤخذ **متزامناً قبل أي await**: `if (_saving \|\| !mounted) return; setState(_saving=true)` |
| **ACCOUNT-SIGN** تحويل رصيد سالب لموجب | ✅ **مُصلَح** | `account_form.dart:244` حقل الرصيد الافتتاحي `signed: true` |
| **BLOCKER-01** تصادم معرّفات جهازين | ✅ **مُصلَح** | `ids.dart`: معرّف Snowflake 63-بت (زمن + بصمة جهاز عشوائية آمنة + عدّاد)؛ اختبار قبول ناجح |
| **BLOCKER-02** سطور الفاتورة لا تُزامَن | ✅ **مُصلَح** | `saveTx` يرفق `items` في payload، و`apply_remote._replaceInvoiceLines` يطبّقها |
| **BLOCKER-03** بيع نقدي FK=0 يكسر القيد | ✅ **مُصلَح** | `saveTx` يحوّل 0→NULL مع `clearAccountId`؛ السماح بالعمليات النقدية بلا حساب |
| **BLOCKER-04** تضارب طابع الوقت (ms مقابل ISO) | ✅ **مُصلَح** | `cloud_firebase_transport`: cursor يحوّل لـ ISO consistently مع ترقيم صفحي overlap |
| **SYNC-LIFECYCLE** مؤقتات لا تُلغى | ✅ **مُصلَح** | `sync_engine.stop()` يلغي `_timer`/`_maintenanceTimer`/`_immediate` ويوقف خادم LAN |
| **SYNC-RETRY** إعادة التقاط بلا حد | ✅ **مُصلَح** | `sync_queue.markFailed`: حد `maxAttempts=12` ثم الحالة `failed` و backoff متزايد |
| **SYNC-LAN** لا مصادقة/أسرار تُسرَّب/بث بلا أجهزة يُعد نجاحاً | ✅ **مُحصّن** | فحص Bearer `auth_secret` + `is_paired` + عدم الطرد؛ إخفاء أسرار الأجهزة الأخرى؛ فحص دور المستخدم عند الكتابة (403) |
| حد حمولة LAN (استنزاف ذاكرة) | ✅ **مُصلَح** | `kMaxLanPayloadBytes` → 413 Request Entity Too Large |
| خادم LAN يعمل تلقائياً | ✅ **مُصلَح** | الافتراضي `lanSyncEnabled ?? '0'` (opt-in) |
| السحابة بلا token / HTTP | ✅ **مُصلَح في التطبيق** | يفرض `https://` ويرسل مصادقة؛ لكن **قواعد الخادم نفسها ضعيفة** (انظر د/1) |
| النسخة تشمل أسرار الأجهزة | ✅ **مُصلَح** | `backupTables` لا تتضمن `devices`/`operations`/`google_auth` |
| activity بعد commit يفشل | ✅ **مُصلَح** | `logActivityTx` داخل نفس `db.transaction` (ذرّي) |
| allBalances بعد الحذف (600 بدل 100) | ✅ **مُصلَح** | الاستعلام يفلتر `COALESCE(deleted_at,'')=''` |
| تحويل لنفس الحساب | ✅ **مُصلَح** | رفض صريح: «اختر حسابي تحويل مختلفين» |
| سحابة: آخر 500 فقط بلا cursor | ✅ **مُصلَح** | سحب تزايدي بـ cursor + ترقيم صفحي |
| LAN: HttpClient جديد لكل جهاز (تسرب) | ✅ **مُصلَح** | عميل واحد يُغلق في `stopServer(force:true)` |

**الخلاصة:** نظام QA الداخلي صارم، وما رُصد من عيوب عولج. اختبارات القبول الأربع للمانعات تنجح.

---

## د) ملاحظات متبقية حقيقية

### 🔴 1) قواعد Firebase Realtime Database — لا عزل بين المستأجرين (الأهم)
`docs/firebase-rules.json`:
```json
".read":  "auth != null && (auth.token.email != null || auth.uid != null)",
".write": "auth != null && (auth.token.email != null || auth.uid != null)"
```
أي **مستخدم سجّل دخول بأي حساب Google** يستطيع القراءة والكتابة على **أي workspace**
بمجرد معرفة/تخمين معرفه — لا يوجد تحقق من المالك (`owner_google_id`) ولا من عضوية الجهاز.
- **الأثر:** تسريب/تعديل بيانات محاسبية لمتاجر أخرى لو نُشرت القواعد كما هي.
- **التوصية:** تقييد القراءة/الكتابة بمالك المساحة أو قائمة أعضاء موثّقة، مثل:
  `".read": "auth != null && data.child('meta/owner').val() === auth.uid"` أو جدول
  `members/$uid` يُتحقَّق منه، مع `".validate": false` لأي مسار غير مُعرَّف.
- ملاحظة: التطبيق نفسه يفرض HTTPS ويرسل التوكن؛ الخلل في **قواعد الخادم** الوثائقية فقط.

### 🟠 2) تجزئة كلمة المرور بملح ثابت عام
`core/security.dart`: `sha256('nexora::' + password)`.
- مناسبة لقفل شاشة محلي، لكنها **غير مملَّحة per-user** وSHA-256 سريع → عرضة لهجمات القاموس
  إن وصلت قاعدة البيانات لخصم (خاصة مع مزامنة/نسخ تتضمن جدول `users`).
- **التوصية:** ملح عشوائي فريد لكل مستخدم يُخزَّن بجانبه، مع دالة بطيئة (PBKDF2/Argon2/scrypt).

### 🟠 3) صيانة التبعيات
`dart pub outdated`: **34 حزمة** متخلّفة عن الأحدث (مثل `google_sign_in 6→7`، `share_plus 10→13`،
`mobile_scanner 5→7`). لا ثغرات مؤكَّدة في النسخ الحالية، لكن النسخ الكبيرة تحتاج اختباراً.
`http ^1.2.2` و`crypto ^3.0.5` و`intl ^0.20.2` ضمن نطاقات حديثة مقبولة.

### 🟡 4) ازدواج مفهومي في المزامنة السحابية
يوجد مساران:
- **الجديد** `cloud_firebase_transport` (Op-log تزايدي، يغذّي المحرك).
- **القديم** `cloud_sync.dart` (دفع **قاعدة كاملة** JSON تحت `/codes/<CODE>.json` بمنطق
  Last-Write-Wins) — ما زال مستدعى من **شاشة النسخ الاحتياطي** فقط.
المسار القديم سليم كنسخة احتياطية يدوية، لكن وجود نقطتَي كتابة سحابيتين قد يربك المستخدم؛
يُفضَّل توضيح الواجهة أو توجيه «السحابة» إلى النسخ الاحتياطي فقط.

### 🟡 5) ملاحظات `flutter analyze` (76، كلها `info`)
أغلبها `prefer_const_constructors` و`curly_braces_in_flow_control_structures` في
`tx_form.dart`/`voucher_doc.dart`/`users_screen.dart`. تجميلية بلا أثر وظيفي؛ تُعالَج بـ
`dart fix --apply` (مع مراجعة الفرق).

### ⚪ 6) حدود بيئة التشغيل/الأمان التشغيلي
- **LAN يستخدم HTTP صراحةً** (تصميم شبكة محلية). البيانات بما فيها `auth_secret` تُمْرَر غير
  مشفّرة على الشبكة المحلية؛ مقبول في شبكة موثوقة، ويجب توضيح ألّا يُفعَّل على شبكة عامة.
- `Access-Control-Allow-Origin: *` على خادم LAN — غير مؤثر عملياً لأنه خادم محلي لا يُستدعى
  من متصفح، لكن يُفضّل تقييده.
- **ميزات لم تُختبَر حياً في هذه البيئة:** اتصال Firebase/Google Drive حقيقي، الكاميرا/البصمة،
  واتساب، الطباعة الحرارية، بناء Android/Windows (لا SDK/محاكي هنا). مُغطّاة باختبارات وحدوية فقط.

---

## هـ) نقاط قوة مؤكَّدة

- **ذرّية كاملة:** الحفظ في معاملة واحدة (العملية + سطور الفاتورة + النشاط + سجل المزامنة)؛
  الاستيراد يُعيد القاعدة كاملة عند أي صف غير صالح.
- **معرّفات عالمية Snowflake** تمنع تصادم الأجهزة، مع بقائها INTEGER (بلا هجرة مخطط).
- **صلاحيات دفاعية:** fail-closed عند المهلة/الاستثناء، حارس «آخر مدير لا يُحذف/يُعطَّل»،
  فحص دور على مستوى خادم LAN أيضاً.
- **لا حقن SQL:** كل الاستعلامات الديناميكية تستخدم `whereArgs` (تحقّقت في المستودع وناقل LAN).
- **لا أسرار في المستودع:** `key.properties`/`*.jks` مستبعدة بـ `.gitignore`، ولا مفاتيح ثابتة في الكود.
- **أمان نسخ:** التصدير يستثني جداول الأسرار، يتحقق من وجود/حجم الصور (≤10MB) ويرفض النسخة الناقصة.
- **تحديث داخل التطبيق:** بيان `version.json` عبر HTTPS + مقارنة SemVer + روابط تحميل لكل منصة.
- **تغطية اختبارية ممتازة:** 167 اختباراً تشمل 16 شاشة بعرضَي 360/1000 بخط عربي حقيقي وقاعدة فعلية،
  اختبارات LAN على loopback بقاعدتين، ودورة حياة المحرك.

---

## و) توصيات ذات أولوية

| الأولوية | الإجراء |
|---|---|
| 🔴 عاجلة | تصحيح قواعد Firebase لعزل workspace بالمالك/الأعضاء قبل أي نشر إنتاجي سحابي |
| 🟠 عالية | ترقية تجزئة كلمات المرور لملح فريد + دالة بطيئة (PBKDF2/Argon2) |
| 🟠 متوسطة | ترقية التبعيات الكبيرة تدريجياً (`google_sign_in`, `share_plus`, `mobile_scanner`) مع تشغيل الاختبارات |
| 🟡 منخفضة | `dart fix --apply` لملاحظات `info`؛ توضيح ازدواج المسار السحابي في الواجهة |
| ⚪ تشغيلية | توثيق أن LAN يُستخدم على شبكة موثوقة فقط؛ اختبار الميزات الحية على جهاز Android حقيقي |

---

## خلاصة

المشروع في حالة **جاهزة للإنتاج على مستوى الكود والمنطق المحلي** (Linux/Android/Windows)،
مع معالجة ناضجة للمعاملات والصلاحيات والمزامنة والنسخ. أهم ما يجب معالجته قبل الاعتماد السحابي
الإنتاجي هو **قواعد Firebase متعددة المستأجرين** (عزل البيانات)، وتحسين تجزئة كلمات المرور.
كل ما عدا ذلك ملاحظات صيانة وتشغيل منخفضة الخطورة.
