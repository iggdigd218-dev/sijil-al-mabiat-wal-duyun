<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- الشبكة: المزامنة السحابية، مزامنة LAN، تسجيل دخول Google، التحديثات -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>

    <!-- البصمة: قفل التطبيق (USE_FINGERPRINT للأجهزة الأقدم من أندرويد 9) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <!-- الكاميرا: مسح الباركود/QR وتصوير سندات العمليات -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- الميكروفون: التسجيل الصوتي (ملاحظات/رسائل صوتية مستقبلاً) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- الإشعارات: أندرويد 13+ يتطلب إذن صريح لعرض الإشعارات -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- الاهتزاز: التفاعلات اللمسية (HapticFeedback) عند الحفظ/الدفع/الحذف/الأخطاء -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- اليقظة داخل المجموعة: خدمة أمامية + قفل يقظة يبقيان المزامنة الفورية
         واستقبال الإشعارات يعملان حتى أثناء سكون الهاتف والشاشة مطفأة -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- إعادة تشغيل خدمة اليقظة بعد إعادة تشغيل الهاتف (داخل المجموعة فقط) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- التحديث بنقرة واحدة: يسمح بفتح شاشة تثبيت النظام لملف APK المنزَّل داخلياً.
         التثبيت النهائي يظل بيد المستخدم (شاشة النظام) — لا تثبيت صامت. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <!-- جهات الاتصال: منتقي النظام الخارجي (openExternalPick) لا يحتاج الإذن نظرياً،
         لكن بعض الأجهزة (خصوصاً بواجهات مصنّعين معدّلة) تشترط READ_CONTACTS
         لقراءة بيانات الجهة المختارة بعد عودتها من المنتقي — نعلنه لضمان العمل. -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!-- الصور والتخزين: اختيار صور من المعرض + حفظ/قراءة النسخ الاحتياطية -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <uses-feature android:name="android.hardware.fingerprint" android:required="false"/>
    <uses-feature android:name="android.hardware.wifi" android:required="false"/>

    <application
        android:label="مدير الحسابات"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:extractNativeLibs="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- خدمة اليقظة: تبقي مزامنة المجموعة والإشعارات فورية والشاشة مطفأة -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- إعادة تشغيل خدمة اليقظة بعد إقلاع الهاتف إن كان الجهاز داخل مجموعة -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- فتح الروابط في المتصفح الخارجي -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <!-- فتح تطبيق الهاتف للاتصال برقم العميل -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- إرسال بريد إلكتروني عبر تطبيق البريد الخارجي -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
        <!-- إرسال رسالة نصية SMS عبر تطبيق الرسائل الخارجي -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="smsto"/>
        </intent>
        <!-- مشاركة الملفات والسندات مع أي تطبيق خارجي -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="*/*"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND_MULTIPLE"/>
            <data android:mimeType="*/*"/>
        </intent>
        <!-- فتح منتقي جهات الاتصال الخارجي (بلا إذن قراءة) على أندرويد 11+ -->
        <intent>
            <action android:name="android.intent.action.PICK"/>
            <data android:mimeType="vnd.android.cursor.dir/contact"/>
        </intent>
        <!-- اختيار ملفات/صور من التطبيقات الخارجية (منتقي الملفات والمعرض) -->
        <intent>
            <action android:name="android.intent.action.GET_CONTENT"/>
            <data android:mimeType="*/*"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT"/>
            <data android:mimeType="*/*"/>
        </intent>
        <!-- فتح روابط whatsapp:// المباشرة (أندرويد 11+ package visibility) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>
        <package android:name="com.whatsapp"/>
        <package android:name="com.whatsapp.w4b"/>
        <package android:name="com.android.contacts"/>
        <package android:name="com.google.android.contacts"/>
    </queries>
</manifest>
