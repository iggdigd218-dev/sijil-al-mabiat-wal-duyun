الإصدار 3.53.0 (بناء 91) — المعمارية الصامتة

- الرابط الرسمي الإقليمي مثبت برمجياً:
  https://nexora-ledger-default-rtdb.europe-west1.firebasedatabase.app
- عزل المساحات: كل تثبيت جديد يولد معرف مساحة فريداً (WS-XXXXXXXX)
  فتعمل كل منشأة في مسار سحابي مستقل تماماً — لا تصادم بيانات.
- ترحيل تلقائي آمن للمعرف القديم default (جهاز منفرد فقط).
- إخفاء بطاقة «المزامنة السحابية (Firebase)» التقنية نهائياً؛
  بقي مدخل «المجموعة وربط الأجهزة» البسيط (QR / رمز دعوة).
- المزامنة التلقائية مثبتة دائماً في الخلفية (autoSync = true).
- تطبيع رابط القاعدة (إزالة الشرطة النهائية) وضبط مسار SSE
  workspaces/{ws}/operations.json — يقضي على sse-http-404.
